name: .Net

on:
  pull_request:
  push:

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    permissions:
      contents: read
      actions: read
      checks: write
    steps:
      - name: Checkout Code
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: 10.x
      - name: Run './build.cmd CI'
        run: ./build.cmd CI
      - name: Upload Test Results
        uses: actions/upload-artifact@v7
        if: ${{ !cancelled() }}
        with:
          name: test-results
          path: ./artifacts/test-results/*.trx
      - uses: dorny/test-reporter@v2
        if: ${{ !cancelled() && github.repository == 'roryclaasen/advent' }}
        with:
          name: Advent of Code Tests
          path: ./artifacts/test-results/*.trx
          reporter: dotnet-trx
          list-tests: failed


  build:
    name: Build ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    needs: test
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            rid: linux-x64
          # - os: macos-latest
          #   rid: osx-x64
          - os: windows-latest
            rid: win-x64
          - os: windows-11-arm
            rid: win-arm64
    steps:
      - name: Checkout Code
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: 10.x
      - name: Run './build.cmd CD'
        run: ./build.cmd CD --rid ${{ matrix.rid }}
      - name: Create Release Artifact
        uses: actions/upload-artifact@v7
        with:
          name: advent-${{ matrix.rid }}
          path: ./artifacts/bin
